providers = ["..."]

[phases.setup]
nixPkgs = ['...']

[phases.build]
cmds = ["...", "php vendor/bin/phinx migrate -e production"]

[start]
cmd = 'perl /assets/prestart.pl /app/nginx.template.conf /nginx.conf && (php-fpm -y /assets/php-fpm.conf & nginx -c /nginx.conf)'